# ===========================================
# MAKER-Council MCP Server - Configuracao
# High Performance Edition
# ===========================================

# --- Credenciais Globais (Auth) ---
# Sua chave de API da Anthropic
ANTHROPIC_API_KEY="sk-ant-..."

# URL base da API (opcional - deixe vazio para usar o padrao)
# Util para proxies ou endpoints alternativos
# Exemplo: ANTHROPIC_BASE_URL="http://localhost:8317/v1"
ANTHROPIC_BASE_URL=""

# --- Configuracao de Modelos (Tiering) ---
# Modelo Inteligente para o Juiz (raciocinio complexo e consenso)
# Recomendado: claude-sonnet-4-5-20250929 (melhor custo-beneficio)
JUDGE_MODEL="claude-sonnet-4-5-20250929"

# Modelo Rapido/Barato para os Voters (geracao de multiplas opcoes)
# Recomendado: claude-3-5-haiku-20241022 (mais economico)
VOTER_MODEL="claude-3-5-haiku-20241022"

# --- Parametros MAKER (Algoritmo) ---
# Margem de votacao first-to-ahead-by-k
# Quanto maior, mais consenso necessario (padrao: 3)
MAKER_K="3"

# Limite de tokens para red-flagging
# Respostas acima deste limite sao descartadas (padrao: 750)
MAKER_MAX_TOKENS="750"

# Maximo de rounds de votacao por processo
# Limite de seguranca para evitar loops infinitos (padrao: 50)
MAKER_MAX_ROUNDS="50"

# --- Parametros de Performance ---
# Numero maximo de requisicoes paralelas a API
# Ajuste baseado nos limites de rate da sua API key (padrao: 10)
MAKER_MAX_CONCURRENT="10"

# Tamanho do lote de votacao paralela
# Quantas amostras disparar simultaneamente por batch (padrao: 5)
MAKER_BATCH_SIZE="5"

# TTL do cache em segundos
# Respostas identicas sao cacheadas por este tempo (padrao: 300 = 5 min)
MAKER_CACHE_TTL="300"

# Tamanho maximo do cache
# Numero maximo de respostas em cache (padrao: 100)
MAKER_CACHE_SIZE="100"

# Habilitar early termination
# Cancela tasks pendentes quando consenso e alcancado (padrao: true)
# Desabilite para debugging ou quando precisar de todas as amostras
MAKER_EARLY_TERMINATION="true"